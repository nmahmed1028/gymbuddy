specVersion: "v1beta"
serviceId: "gymbuddy-data-connect"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "gymbuddy-db"
      cloudSql:
        instanceId: "gymbuddy-sql"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
